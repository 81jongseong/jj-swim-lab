# 🔍 AntiGravity를 위한 JJ Swim Lab 프로젝트 안정성 & 최적화 점검 가이드

> **목적**: 이 문서를 AntiGravity에게 전달하면 프로젝트의 안정성, 최적화, 코드 품질을 체계적으로 점검할 수 있습니다.  
> **사용법**: AntiGravity에게 이 문서의 내용을 전달하고 점검을 요청하세요.

---

## 📋 **점검 항목 개요**

### **점검 카테고리**
1. ✅ **빌드 안정성** - 빌드 오류, 컴파일 오류
2. ✅ **타입 안전성** - TypeScript 오류, 타입 정의
3. ✅ **코드 품질** - 린트 오류, 코드 스타일
4. ✅ **성능 최적화** - 번들 크기, 로딩 속도, 렌더링 성능
5. ✅ **보안** - 보안 취약점, 의존성 업데이트
6. ✅ **테스트** - 테스트 커버리지, 테스트 실행
7. ✅ **프로젝트 구조** - 파일 구조, 중복 코드
8. ✅ **문서화** - 주석, 문서 완성도

---

## 🚀 **1. 빌드 안정성 점검**

### **1.1 서버 빌드 점검**

**명령어:**
```bash
cd server
npm run build
```

**점검 항목:**
- ✅ TypeScript 컴파일 오류 확인
- ✅ 빌드 출력 파일 생성 확인 (`dist/` 폴더)
- ✅ 빌드 경고 메시지 확인
- ✅ 빌드 시간 확인 (비정상적으로 오래 걸리는지)

**문제 발견 시:**
- 빌드 오류 메시지 기록
- 오류가 발생한 파일 경로 및 라인 번호 기록
- `server/tsconfig.json` 설정 확인

**확인 파일:**
- `server/tsconfig.json`
- `server/package.json` (build 스크립트)
- `server/dist/` (빌드 출력 디렉토리)

---

### **1.2 클라이언트 빌드 점검**

**명령어:**
```bash
cd client
npm run build
```

**점검 항목:**
- ✅ Next.js 빌드 성공 여부
- ✅ 빌드 오류 및 경고 확인
- ✅ `.next/` 폴더 생성 확인
- ✅ 빌드 통계 확인 (페이지 수, 번들 크기)
- ✅ 정적 생성 페이지 확인

**문제 발견 시:**
- 빌드 오류 메시지 기록
- 실패한 페이지 경로 기록
- `next.config.js` 설정 확인

**확인 파일:**
- `client/next.config.js`
- `client/package.json` (build 스크립트)
- `client/.next/` (빌드 출력 디렉토리)

---

## 📝 **2. 타입 안전성 점검**

### **2.1 서버 TypeScript 타입 체크**

**명령어:**
```bash
cd server
npm run type-check
# 또는
npx tsc --noEmit
```

**점검 항목:**
- ✅ 타입 오류 개수 확인 (0개가 목표)
- ✅ 타입 경고 메시지 확인
- ✅ `any` 타입 사용 최소화 여부
- ✅ 타입 정의 파일 완성도

**문제 발견 시:**
- 타입 오류 목록 작성 (파일 경로, 라인 번호, 오류 메시지)
- `any` 타입 사용 위치 기록
- 누락된 타입 정의 확인

**확인 파일:**
- `server/src/**/*.ts` (모든 TypeScript 파일)
- `server/src/types/` (타입 정의 파일)
- `server/tsconfig.json`

---

### **2.2 클라이언트 TypeScript 타입 체크**

**명령어:**
```bash
cd client
npm run type-check
# 또는
npx tsc --noEmit
```

**점검 항목:**
- ✅ 타입 오류 개수 확인 (0개가 목표)
- ✅ 타입 경고 메시지 확인
- ✅ React 컴포넌트 타입 안전성
- ✅ Props 타입 정의 완성도

**문제 발견 시:**
- 타입 오류 목록 작성
- 컴포넌트 Props 타입 누락 확인
- API 응답 타입 정의 확인

**확인 파일:**
- `client/app/**/*.tsx` (모든 React 컴포넌트)
- `client/components/**/*.tsx`
- `client/lib/**/*.ts`
- `client/types/**/*.ts`

---

## 🔍 **3. 코드 품질 점검**

### **3.1 ESLint 린트 체크**

**서버 린트:**
```bash
cd server
npm run lint
```

**클라이언트 린트:**
```bash
cd client
npm run lint
```

**점검 항목:**
- ✅ 린트 오류 개수 확인 (0개가 목표)
- ✅ 린트 경고 메시지 확인
- ✅ 코드 스타일 일관성
- ✅ 사용하지 않는 변수/import 확인

**문제 발견 시:**
- 린트 오류 목록 작성
- 자동 수정 가능한 오류 확인 (`npm run lint:fix` 실행 가능 여부)
- ESLint 설정 파일 확인

**확인 파일:**
- `server/eslint.config.mjs`
- `client/eslint.config.mjs`
- `.eslintignore` 파일

---

### **3.2 코드 스타일 점검**

**점검 항목:**
- ✅ 파일 상단 주석 존재 여부 (연동 데이터, 연동 파일 주석)
- ✅ 일관된 코드 포맷팅 (들여쓰기, 세미콜론 등)
- ✅ 명명 규칙 일관성 (변수명, 함수명)
- ✅ 중복 코드 존재 여부

**확인 방법:**
- 랜덤 샘플 파일 검토 (각 디렉토리에서 5-10개 파일)
- 파일 상단 주석 확인

**확인 파일:**
- `DEVELOPMENT.md` (코딩 규칙)
- `docs/개발-주석-가이드.md`

---

## ⚡ **4. 성능 최적화 점검**

### **4.1 번들 크기 분석**

**클라이언트 번들 분석:**
```bash
cd client
npm run build:analyze
# 또는
ANALYZE=true npm run build
```

**점검 항목:**
- ✅ 각 페이지 번들 크기 확인
- ✅ 큰 의존성 라이브러리 확인 (100KB 이상)
- ✅ 중복 번들 확인
- ✅ Tree shaking 적용 여부

**문제 발견 시:**
- 큰 번들 파일 목록 작성
- 개선 제안 (코드 스플리팅, 동적 import 등)

**확인 파일:**
- `client/next.config.js` (번들 최적화 설정)
- `.next/analyze/` (번들 분석 결과)

---

### **4.2 이미지 및 정적 자산 최적화**

**점검 항목:**
- ✅ 이미지 최적화 적용 여부 (Next.js Image 컴포넌트)
- ✅ 불필요한 정적 파일 확인
- ✅ 폰트 최적화 확인

**확인 디렉토리:**
- `client/public/`
- `client/app/**/*.tsx` (Image 컴포넌트 사용)

---

### **4.3 API 성능 점검**

**점검 항목:**
- ✅ API 응답 시간 확인 (느린 엔드포인트)
- ✅ 데이터베이스 쿼리 최적화 여부
- ✅ 캐싱 전략 확인
- ✅ N+1 쿼리 문제 확인

**확인 파일:**
- `server/src/routes/**/*.ts` (API 라우트)
- `server/src/models/**/*.ts` (Mongoose 모델)

---

### **4.4 렌더링 성능 점검**

**점검 항목:**
- ✅ 불필요한 리렌더링 확인 (React DevTools)
- ✅ 메모이제이션 적용 여부 (`useMemo`, `useCallback`)
- ✅ 코드 스플리팅 적용 여부 (`dynamic import`)

**확인 파일:**
- `client/app/**/*.tsx` (페이지 컴포넌트)
- `client/components/**/*.tsx` (재사용 컴포넌트)

---

## 🔒 **5. 보안 점검**

### **5.1 의존성 취약점 점검**

**명령어:**
```bash
# 루트 디렉토리
npm audit

# 서버
cd server
npm audit

# 클라이언트
cd client
npm audit
```

**점검 항목:**
- ✅ 보안 취약점 개수 확인
- ✅ Critical/High 취약점 확인
- ✅ 오래된 패키지 확인

**문제 발견 시:**
- 취약점 목록 작성
- 업데이트 가능한 패키지 확인

---

### **5.2 환경 변수 및 시크릿 점검**

**점검 항목:**
- ✅ `.env` 파일 `.gitignore` 포함 여부
- ✅ 하드코딩된 시크릿 키 확인
- ✅ 환경 변수 사용 확인

**확인 파일:**
- `.gitignore`
- `.env.example`
- `server/src/**/*.ts` (하드코딩된 시크릿 검색)

---

### **5.3 API 보안 점검**

**점검 항목:**
- ✅ 인증 미들웨어 적용 여부
- ✅ 권한 검증 확인 (`requireRole`)
- ✅ CORS 설정 확인
- ✅ Rate limiting 적용 여부

**확인 파일:**
- `server/src/middleware/auth.ts`
- `server/src/middleware/role.ts`
- `server/src/index.ts` (CORS, Rate limiting 설정)

---

## 🧪 **6. 테스트 점검**

### **6.1 테스트 실행**

**서버 테스트:**
```bash
cd server
npm test
```

**클라이언트 테스트:**
```bash
cd client
npm test
```

**점검 항목:**
- ✅ 테스트 실행 성공 여부
- ✅ 테스트 실패 케이스 확인
- ✅ 테스트 커버리지 확인
- ✅ 테스트 작성 비율 확인

**문제 발견 시:**
- 실패한 테스트 목록 작성
- 테스트 커버리지 리포트 작성

**확인 파일:**
- `server/src/**/*.test.ts`
- `client/__tests__/**/*.test.tsx`
- `coverage/` (테스트 커버리지 리포트)

---

### **6.2 E2E 테스트 점검**

**명령어:**
```bash
cd client
npm run test:e2e
```

**점검 항목:**
- ✅ E2E 테스트 실행 성공 여부
- ✅ 주요 플로우 테스트 커버리지
- ✅ 테스트 안정성 확인

**확인 파일:**
- `client/e2e/**/*.spec.ts`

---

## 📁 **7. 프로젝트 구조 점검**

### **7.1 디렉토리 구조 확인**

**점검 항목:**
- ✅ 디렉토리 구조 일관성
- ✅ 파일 명명 규칙 일관성
- ✅ 중복 파일 확인

**확인 방법:**
- `docs/프로젝트-구조.md`와 실제 구조 비교
- 중복된 파일 검색

---

### **7.2 코드 중복 점검**

**점검 항목:**
- ✅ 중복 함수/컴포넌트 확인
- ✅ 유사한 로직 통합 가능 여부
- ✅ 공통 유틸리티 활용 확인

**확인 파일:**
- `client/lib/utils.ts`
- `server/src/utils/`

---

### **7.3 의존성 점검**

**점검 항목:**
- ✅ 사용하지 않는 패키지 확인
- ✅ 중복된 패키지 확인
- ✅ 패키지 버전 충돌 확인

**확인 파일:**
- `package.json`
- `server/package.json`
- `client/package.json`
- `pnpm-lock.yaml`

---

## 📚 **8. 문서화 점검**

### **8.1 코드 주석 점검**

**점검 항목:**
- ✅ 파일 상단 주석 존재 여부 (연동 데이터, 연동 파일)
- ✅ 함수/클래스 주석 완성도
- ✅ 복잡한 로직 주석 존재 여부

**확인 방법:**
- 랜덤 샘플 파일 검토
- `docs/개발-주석-가이드.md` 기준 확인

---

### **8.2 프로젝트 문서 점검**

**점검 항목:**
- ✅ 필수 문서 존재 여부
- ✅ 문서 최신성 확인
- ✅ 문서 완성도 확인

**확인 파일:**
- `PROJECT_CONTEXT.md`
- `DEVELOPMENT.md`
- `README.md`
- `docs/` 디렉토리

---

## 📊 **9. 점검 결과 보고서 형식**

AntiGravity가 점검 후 다음과 같은 형식으로 보고서를 작성하도록 요청:

```markdown
# 🔍 JJ Swim Lab 프로젝트 점검 결과 보고서

**점검 일시**: 2025-XX-XX
**점검자**: AntiGravity

---

## ✅ 통과 항목

### 빌드 안정성
- [ ] 서버 빌드: ✅ 성공
- [ ] 클라이언트 빌드: ✅ 성공

### 타입 안전성
- [ ] 서버 타입 오류: 0개
- [ ] 클라이언트 타입 오류: 0개

### 코드 품질
- [ ] 서버 린트 오류: 0개
- [ ] 클라이언트 린트 오류: 0개

---

## ⚠️ 개선 필요 항목

### [항목명]
- **심각도**: High/Medium/Low
- **위치**: 파일 경로, 라인 번호
- **문제 설명**: 
- **개선 제안**: 

---

## 📈 성능 최적화 제안

### 번들 크기
- 현재: XXX KB
- 제안: XXX KB (개선 방법)

### API 성능
- 느린 엔드포인트 목록
- 개선 제안

---

## 🔒 보안 취약점

### 의존성 취약점
- Critical: X개
- High: X개
- 업데이트 제안

---

## 💡 우선순위별 개선 제안

### 🔴 긴급 (즉시 수정 필요)
1. 

### 🟡 중요 (빠른 시일 내 수정 권장)
1. 

### 🟢 권장 (점진적 개선)
1. 
```

---

## 🎯 **10. AntiGravity에게 전달할 프롬프트**

다음 프롬프트를 AntiGravity에게 전달하세요:

```
안녕하세요! JJ Swim Lab 프로젝트의 안정성과 최적화를 점검해주세요.

다음 항목들을 체계적으로 점검해주세요:

1. 빌드 안정성
   - server와 client 디렉토리에서 각각 `npm run build` 실행
   - 빌드 오류 및 경고 확인

2. 타입 안전성
   - server와 client에서 각각 `npm run type-check` 실행
   - 타입 오류 개수 및 상세 목록 확인

3. 코드 품질
   - server와 client에서 각각 `npm run lint` 실행
   - 린트 오류 개수 및 자동 수정 가능 여부 확인

4. 성능 최적화
   - client에서 `npm run build:analyze` 실행하여 번들 크기 분석
   - 큰 의존성 라이브러리 확인
   - API 성능 확인

5. 보안
   - 각 디렉토리에서 `npm audit` 실행
   - 보안 취약점 확인 및 업데이트 제안

6. 테스트
   - server와 client에서 각각 `npm test` 실행
   - 테스트 커버리지 확인

7. 프로젝트 구조
   - 문서화된 구조와 실제 구조 비교
   - 중복 코드 및 파일 확인

8. 문서화
   - 파일 상단 주석 존재 여부 확인
   - 프로젝트 문서 완성도 확인

점검 후 "ANTIGRAVITY_AUDIT_GUIDE.md" 파일의 "9. 점검 결과 보고서 형식"에 따라 
상세한 보고서를 작성해주세요. 특히 다음을 포함해주세요:

- 통과한 항목
- 개선이 필요한 항목 (심각도별 분류)
- 성능 최적화 제안
- 보안 취약점 목록
- 우선순위별 개선 제안

각 항목에 대해 구체적인 파일 경로, 라인 번호, 개선 제안을 포함해주세요.
```

---

## 📝 **11. 추가 점검 스크립트**

프로젝트에 다음 스크립트들이 있으니 참고하세요:

- `check.bat` - 통합 검증 스크립트 (Windows)
- `scripts/check-all.js` - 통합 검증 스크립트 (Node.js)
- `scripts/quick-check.js` - 빠른 점검 스크립트

이 스크립트들을 실행하여 전체적인 건강 상태를 확인할 수 있습니다.

---

**마지막 업데이트**: 2025-11-18  
**작성자**: JJ Swim Lab 개발팀

