/**
 * 🔧 JJ Swim Lab - TypeScript 설정 파일
 * 
 * 📋 **파일 목적**
 * - TypeScript 컴파일러의 핵심 설정 및 옵션을 정의하는 설정 파일
 * - 타입 검사 규칙, 컴파일 옵션, 경로 별칭 등의 설정 관리
 * - 개발 환경별 타입 검사 및 컴파일 설정 분기
 * - 프로젝트 구조에 맞는 경로 매핑 및 모듈 해석 설정
 * - 타입 안전성 및 개발 생산성 향상을 위한 설정 제공
 * 
 * 🔄 **주요 기능**
 * - 타입 검사 규칙 및 엄격성 설정
 * - 컴파일 대상 및 출력 설정
 * - 경로 별칭 및 모듈 해석 설정
 * - 라이브러리 타입 정의 설정
 * - 개발 환경별 설정 분기
 * - 타입 체크 성능 최적화
 * 
 * 🗄️ **데이터 연동**
 * - TypeScript 컴파일러 설정
 * - 타입 정의 파일 및 라이브러리
 * - 경로 매핑 및 모듈 해석
 * - 타입 검사 규칙 및 옵션
 * - 개발 환경 설정
 * 
 * 🛠️ **필요한 설치 파일**
 * - TypeScript 컴파일러
 * - 타입 정의 파일 (.d.ts)
 * - 경로 별칭 해석 도구
 * - 타입 검사 및 린팅 도구
 * - 개발 환경 설정 도구
 * 
 * ⚠️ **개발 시 주의사항**
 * 1. 타입 검사 규칙의 적절성 및 일관성 유지
 * 2. 경로 별칭 설정의 정확성 및 중복 방지
 * 3. 컴파일 옵션의 성능 및 호환성 고려
 * 4. 타입 정의 파일의 버전 호환성 확인
 * 5. 개발 환경별 설정의 일관성 유지
 * 
 * 🔧 **수정 시 체크리스트**
 * - [ ] 타입 검사 규칙 및 엄격성 설정 확인
 * - [ ] 경로 별칭 및 모듈 해석 설정 검증
 * - [ ] 컴파일 옵션 및 출력 설정 확인
 * - [ ] 타입 정의 파일 및 라이브러리 설정 확인
 * - [ ] 개발 환경별 설정 분기 확인
 * 
 * 📅 **개발 히스토리**
 * - 2024-12-19: 초기 구현 (기본 TypeScript 설정)
 * - 2024-12-19: 타입 검사 규칙 및 엄격성 시스템 구현
 * - 2024-12-19: 경로 별칭 및 모듈 해석 시스템 구현
 * - 2024-12-19: 개발 환경별 설정 분기 시스템 구현
 * 
 * 👨‍💻 **개발자 정보**
 * - 작성자: AI Assistant
 * - 최종 수정: 2024-12-19
 * - 상태: ✅ 완성 (TypeScript 설정 시스템 완료)
 * 
 * 🚀 **다음 단계**
 * - AI 기반 타입 검사 최적화
 * - 자동 타입 정의 생성
 * - 성능 최적화
 * - 타입 안전성 강화
 * 
 * 💡 **주요 설정 옵션**
 * - compilerOptions: 컴파일러 옵션 및 타입 검사 규칙
 * - include: 컴파일 대상 파일 및 디렉토리
 * - exclude: 컴파일 제외 파일 및 디렉토리
 * - paths: 경로 별칭 및 모듈 해석 설정
 * - baseUrl: 기본 경로 및 모듈 해석 기준
 * 
 * 🔍 **설정 처리 흐름**
 * 1. 기본 TypeScript 설정 로드
 * 2. 타입 검사 규칙 및 엄격성 설정 적용
 * 3. 경로 별칭 및 모듈 해석 설정 적용
 * 4. 컴파일 옵션 및 출력 설정 적용
 * 5. 최종 TypeScript 설정 객체 반환
 */

{
  "compilerOptions": {
    "target": "es2015",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": false,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": false,
    "downlevelIteration": true,
    "forceConsistentCasingInFileNames": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"],
      "@/components/*": ["./components/*"],
      "@/lib/*": ["./lib/*"],
      "@/app/*": ["./app/*"],
      "@/hooks/*": ["./hooks/*"],
      "@/types/*": ["./types/*"],
      "@/utils/*": ["./utils/*"]
    },
    "typeRoots": ["./node_modules/@types"]
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts", "node_modules/@types/react/index.d.ts"],
  "exclude": ["node_modules", "**/*.test.ts", "**/*.test.tsx", "**/__tests__/**/*"]
}
