# 🏗️ JJ Swim Lab - 프로젝트 구조

**최종 업데이트**: 2025-09-18  
**문서 버전**: 1.3.0

> **🔧 최신 업데이트**: TypeScript 오류 해결, PWA 오프라인 기능 확인, 계정별 기능명세서 추가

## 📁 **전체 프로젝트 구조**

```
jj-swim-lab/
├── client/                 # Next.js 프론트엔드
├── server/                 # Express.js 백엔드
├── docs/                   # 프로젝트 문서
│   ├── 계정별-기능명세서.md    # 계정별 기능 현황 (NEW)
│   ├── PWA-오프라인-가이드.md  # PWA 오프라인 사용법 (NEW)
│   ├── DEVELOPMENT.md         # 개발 가이드 및 오류 히스토리
│   ├── API-문서.md           # API 엔드포인트 문서
│   ├── WORK_HISTORY.md       # 작업 히스토리
│   ├── 프로젝트-요약.md       # 프로젝트 전체 요약
│   ├── 프로젝트-구조.md       # 프로젝트 구조 설명
│   └── 현재-작업-상태.md      # 현재 작업 상태
├── uploads/                # 파일 업로드 디렉토리
└── scripts/                # 유틸리티 스크립트
```

## 🖥️ **클라이언트 구조 (포트 3000)**

### **📱 App Router 구조**
```
client/app/
├── (labs)/                 # 실험실 기능
├── about/                  # 소개 페이지
├── accessibility/          # 접근성 관련
├── admin/                  # 관리자 전용 페이지 ⭐
│   ├── dashboard/         # 메인 대시보드
│   ├── revenue/           # 총 매출 관리 (신규) ⭐
│   ├── approvals/         # 승인 대기 관리 (신규) ⭐
│   └── instructor-management/ # 강사 관리
├── ai-analysis/            # AI 분석 기능
├── ai-config/              # AI 설정
├── api/                    # API 라우트
├── auth/                   # 인증 관련
├── bookings/               # 예약 관리
├── community/              # 커뮤니티
├── courses/                # 과정 관리
├── dashboard/              # 사용자 대시보드
├── error.tsx               # 에러 페이지
├── global-error.tsx        # 전역 에러 페이지
├── globals.css             # 전역 스타일
├── guide/                  # 가이드
├── health/                 # 건강 관리
├── install/                # 설치 가이드
├── instructor/             # 강사 전용
│   └── health/            # 강사 건강 관리
│       ├── overview/       # 개요
│       ├── progress/       # 진행 상황
│       ├── recommendations/ # AI 추천
│       └── students/       # 학생 관리
├── layout.tsx              # 루트 레이아웃
├── loading.tsx             # 로딩 페이지
├── localization/            # 다국어 지원
├── map/                    # 지도 기능
├── membership/             # 멤버십 관리
├── news/                   # 뉴스
├── notifications/          # 알림
├── offline/                # 오프라인 기능
├── page.tsx                # 홈페이지
├── payments/               # 결제 관리
├── personalized-dashboard/ # 개인화 대시보드
├── quiz/                   # 퀴즈 시스템
├── shop/                   # 상점
├── sw-register.tsx         # 서비스 워커 등록
├── uploads/                # 파일 업로드
└── user-role-integration/  # 사용자 역할 통합
```

### **🧩 컴포넌트 구조**
```
client/components/
├── ui/                     # 기본 UI 컴포넌트
│   ├── Button.tsx         # 버튼 컴포넌트
│   ├── Card.tsx           # 카드 컴포넌트
│   ├── Badge.tsx          # 배지 컴포넌트
│   ├── Input.tsx          # 입력 필드
│   ├── Select.tsx         # 선택 박스
│   ├── Modal.tsx          # 모달
│   ├── Tabs.tsx           # 탭 컴포넌트
│   └── index.ts           # UI 컴포넌트 인덱스
├── Navigation.tsx          # 네비게이션
├── TopNavigation.tsx       # 상단 네비게이션
├── DynamicNavigation.tsx   # 동적 네비게이션
├── ResponsiveTable.tsx     # 반응형 테이블
├── DashboardLayout.tsx     # 대시보드 레이아웃
├── UserDashboard.tsx       # 사용자 대시보드
├── AIDashboard.tsx         # AI 대시보드
├── ExerciseDashboard.tsx   # 운동 대시보드
├── HealthProfileForm.tsx   # 건강 프로필 폼
├── ExcelUploader.tsx       # Excel 업로더
├── NotificationsBell.tsx   # 알림 벨
├── OfflineIndicator.tsx    # 오프라인 표시기
├── PWAInstallPrompt.tsx    # PWA 설치 프롬프트
├── LottiePlayer.tsx        # Lottie 애니메이션
├── ThreeSplash.tsx         # 3D 스플래시
├── WaterRippleBackground.tsx # 물결 배경
└── withAuth.tsx            # 인증 HOC
```

### **🔧 유틸리티 및 훅**
```
client/
├── hooks/
│   ├── useAuth.tsx         # 인증 훅
│   ├── useLocalStorage.ts  # 로컬 스토리지 훅
│   └── useOffline.ts       # 오프라인 상태 훅
├── lib/
│   ├── api/                # API 클라이언트
│   │   ├── auth.ts         # 인증 API
│   │   ├── dashboard.ts    # 대시보드 API (신규) ⭐
│   │   └── instructorManagement.ts # 강사 관리 API
│   ├── mockData.ts         # 목 데이터 (삭제됨)
│   ├── motion.ts           # 애니메이션 설정
│   ├── offlineDB.ts        # 오프라인 데이터베이스
│   ├── socket.ts           # 소켓 연결
│   └── utils.ts            # 유틸리티 함수
├── utils/
│   └── helpers.ts          # 헬퍼 함수
└── styles/
    └── theme.ts            # 테마 설정
```

## 🖥️ **서버 구조 (포트 5000)**

### **📂 소스 코드 구조**
```
server/src/
├── models/                 # Mongoose 모델
│   ├── User.ts            # 사용자 모델
│   ├── HealthProfile.ts   # 건강 프로필 모델
│   ├── HealthMetrics.ts   # 건강 지표 모델
│   ├── ExerciseRecord.ts  # 운동 기록 모델
│   ├── AIRecommendation.ts # AI 추천 모델
│   ├── Course.ts          # 과정 모델
│   ├── Booking.ts         # 예약 모델
│   ├── Payment.ts         # 결제 모델
│   ├── Approval.ts        # 승인 모델 (신규) ⭐
│   ├── Checklist.ts       # 체크리스트 모델
│   └── TeachingMethod.ts  # 강습법 모델
├── routes/                 # API 라우트
│   ├── auth.ts            # 인증 라우트
│   ├── users.ts           # 사용자 관리
│   ├── health.ts          # 건강 관리
│   ├── instructorManagement.ts # 강사 관리
│   ├── dashboard.ts       # 대시보드 API (신규) ⭐
│   ├── courses.ts         # 과정 관리
│   ├── bookings.ts        # 예약 관리
│   ├── payments.ts        # 결제 관리
│   └── approvals.ts       # 승인 관리 (신규) ⭐
├── middleware/             # 미들웨어
│   ├── auth.ts            # 인증 미들웨어
│   ├── role.ts            # 역할 기반 접근 제어
│   └── validation.ts      # 데이터 검증
├── utils/                  # 유틸리티
│   ├── database.ts        # 데이터베이스 연결
│   ├── logger.ts          # 로깅
│   └── helpers.ts         # 헬퍼 함수
├── config/                 # 설정
│   ├── database.ts        # 데이터베이스 설정
│   └── server.ts          # 서버 설정
└── index.ts               # 메인 서버 파일
```

### **📜 스크립트 및 설정**
```
server/
├── scripts/                # 데이터베이스 스크립트
│   ├── seed-health-data.js # 건강 데이터 시드 (신규) ⭐
│   ├── seed-dashboard-data.js # 대시보드 데이터 시드 (신규) ⭐
│   ├── create-admin.js     # 관리자 계정 생성 (신규) ⭐
│   ├── check-db-status.js  # 데이터베이스 상태 확인 (신규) ⭐
│   ├── drop-user-index.js  # 사용자 인덱스 삭제
│   └── deploy.ps1         # 배포 스크립트
├── uploads/                # 파일 업로드 디렉토리
├── dist/                   # 컴파일된 JavaScript
├── logs/                   # 로그 파일
├── public/                 # 정적 파일
├── .env.example           # 환경 변수 예시
├── nodemon.json           # nodemon 설정
├── tsconfig.json          # TypeScript 설정
└── package.json           # 의존성 관리
```

## 📚 **문서 구조**

```
docs/
├── API-문서.md             # API 명세서
├── TODO.md                 # 할 일 목록
├── 프로젝트-가이드.md      # 프로젝트 가이드
├── 프로젝트-구조.md        # 프로젝트 구조 (현재 문서)
├── 프로젝트-요약.md        # 프로젝트 요약
├── 현재-작업-상황.md       # 현재 작업 상황
└── 현재-작업-상황.md       # 현재 작업 상황 (중복)
```

## 🔄 **최근 구조 변경사항**

### **✅ 2024년 1월 15일 - 새로운 관리 페이지 추가**
- **`/admin/revenue`**: 총 매출 관리 페이지
- **`/admin/approvals`**: 승인 대기 관리 페이지
- **`server/src/models/Approval.ts`**: 승인 모델 추가
- **`server/src/routes/dashboard.ts`**: 대시보드 API 추가
- **`client/lib/api/dashboard.ts`**: 대시보드 API 클라이언트 추가

### **✅ 2024년 12월 - 구조 개선**
- **동적 네비게이션 시스템**: 사용자 권한별 네비게이션 선택
- **모델 export 표준화**: named export로 통일
- **타입 안전성 강화**: TypeScript 컴파일 오류 해결

## 🎯 **주요 특징**

### **🏗️ 모듈화된 구조**
- **클라이언트**: Next.js App Router 기반 페이지별 구조
- **서버**: Express.js 기반 라우터별 API 구조
- **컴포넌트**: 재사용 가능한 UI 컴포넌트 분리

### **🔐 권한 기반 접근 제어**
- **사용자 유형별**: superAdmin, centerAdmin, instructor, student, guest
- **기능별 접근**: 권한에 따른 페이지 및 API 접근 제어
- **동적 네비게이션**: 사용자 권한에 따른 메뉴 표시

### **📱 반응형 및 PWA 지원**
- **모바일 최적화**: Tailwind CSS 기반 반응형 디자인
- **PWA 기능**: 오프라인 지원 및 앱 설치 프롬프트
- **애니메이션**: Framer Motion 기반 부드러운 인터랙션

## 🚀 **확장성 고려사항**

### **📈 수평 확장**
- **마이크로서비스**: 기능별 서비스 분리 가능
- **데이터베이스**: MongoDB Atlas 클러스터 확장
- **캐싱**: Redis 추가로 성능 향상

### **🔌 플러그인 시스템**
- **모듈화된 컴포넌트**: 새로운 기능 쉽게 추가
- **API 확장**: 새로운 엔드포인트 추가 용이
- **테마 시스템**: 다중 테마 지원 가능

---
*마지막 업데이트: 2024년 1월 15일*
*작성자: AI Assistant*
